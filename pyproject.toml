[project]
name = "quest-npc-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ahfmrptEkd" }
]
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.15.0",
    "datasets>=5.0.1",
    "peft>=0.20.0",
    "pytest>=9.1.1",
    "torch>=2.14.0",
    "transformers>=5.16.1",
    "trl>=1.12.0",
]

[project.scripts]
quest-npc-lab = "quest_npc_lab:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
